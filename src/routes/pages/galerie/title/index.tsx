import { component$ } from '@builder.io/qwik';

export const Title = component$(() => {
    return (
        <section id="title" class="container">
            <h1>Galerie</h1>
        </section>
    )
});